[project]
name = "backend-django"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "Django>=5.1,<6.0",
    "djangorestframework>=3.15",
    "django-cors-headers>=4.6",
    "python-dotenv>=1.0",
    "djangorestframework-simplejwt",
    "bcrypt",
    "pymongo",
    "redis",

    # 추가
    "neo4j",
    "openpyxl"
]